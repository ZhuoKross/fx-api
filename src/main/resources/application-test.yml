server:
  port: 9094

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_dev
    username: michael
    password: jinwoo2912
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:

logging:
  level:
    org.flywaydb: debug